spring:
  application:
    name: lostfound
  config:
    import: optional:file:./config/application-local.yml,optional:classpath:application-local.yml
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
  cloud:
    aws:
      s3:
        region: us-east-1

app:
  s3:
    bucket: lostfoundstorage

file:
  upload-dir: ./uploads

server:
  port: ${SERVER_PORT:${PORT:5000}}
